[project]
name = "fabv2-evals"
version = "0.1.0"
description = "Frozen FAB v2 evaluator for the Prime self-harness loop"
requires-python = ">=3.12,<3.14"
dependencies = []

[dependency-groups]
test = [
    "pytest>=8.4",
    "pytest-timeout>=2.3",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
timeout = 900
